# Client — where the contact form posts. Leave empty and the form falls back
# to opening a prefilled mail draft, so the page always converts.
VITE_CONTACT_ENDPOINT=

# Server (api/contact.ts) — never expose these to the client.
RESEND_API_KEY=
CONTACT_TO=darien.cepani42@gmail.com
CONTACT_FROM=Vela <hello@vela.al>
ALLOWED_ORIGIN=https://vela.al
